import { Outlet } from 'react-router-dom';

const Home = () => {
  return (
    <div>
        {/* <Outlet />  This is used to render the child elements of the parent component */}
      <h1>Home Page</h1>
    </div>
  );
};

export default Home;
