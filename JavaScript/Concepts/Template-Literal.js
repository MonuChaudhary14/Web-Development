//Template literals are a modern way to create strings in JavaScript using backticks (`) instead of single (') or double (") quotes.

// They make string creation easier, more readable, and more powerful.

let message = `This message will comes in 
the same way, it is written 
here`;

console.log(message);

// This is mostly used when we are emailiing for performing more details

let fullname = "Monu Chaudhary";
let greet = `Hello, ${fullname} Glad to meet you`;

console.log(greet);