// An HTMLCollection is an array-like object that contains a list of HTML elements. It is live, meaning it updates automatically when the DOM changes.

// You often get an HTMLCollection when using certain DOM methods like:

// document.getElementsByTagName()

// document.getElementsByClassName()

// element.children