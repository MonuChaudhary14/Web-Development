// JavaScript is single-threaded, meaning it executes one command at a time in a single call stack.

// Only one task runs at a time.

// Code is executed line-by-line (synchronously) in the main thread.

// JavaScript does not do true parallel execution (like multithreading in Java, C++), but it can simulate concurrency via asynchronous programming.

